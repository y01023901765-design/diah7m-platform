# ═══════════════════════════════════════════════════════════
# DIAH-7M Server Environment Variables
# ═══════════════════════════════════════════════════════════
# 복사: cp .env.example .env  →  값 채우기
# 서버 시작 시 필수 키 누락이면 즉시 종료 (fail-fast)

NODE_ENV=production
PORT=3700
JWT_SECRET=                    # openssl rand -hex 32

# ── 데이터 수집 API 키 (Phase 1 필수) ──
ECOS_API_KEY=              # 한국은행 ECOS (무료): https://ecos.bok.or.kr/api
KOSIS_API_KEY=             # 통계청 KOSIS (무료): https://kosis.kr/openapi
FRED_API_KEY=              # 미국 FRED (무료): https://fred.stlouisfed.org/docs/api/api_key.html
AIRKOREA_API_KEY=          # 공공데이터 (무료): https://data.go.kr → "대기오염정보 조회 서비스"

# ── 결제 (Phase 2 — Stripe) ──
STRIPE_SECRET_KEY=         # Stripe 비밀키 (sk_live_...)
STRIPE_WEBHOOK_SECRET=     # 웹훅 시크릿 (whsec_...)
STRIPE_PRICE_BASIC=        # 가격 ID (price_...)
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=

# ── 이메일 (Phase 2 — SendGrid) ──
SENDGRID_API_KEY=          # SendGrid API 키
SENDGRID_FROM=noreply@diah7m.com

# ── 데이터베이스 ──
DATABASE_URL=              # PostgreSQL (없으면 SQLite 자동 전환)

# ── Google Earth Engine (Phase 2 — 위성) ──
GEE_SERVICE_ACCOUNT=       # GEE 서비스 계정 이메일
GEE_KEY_FILE=              # 1순위: Secret File 경로 (Render: /etc/secrets/gee-sa.json)
GEE_CREDENTIALS_B64=       # 2순위: 서비스계정 JSON을 base64로 (cat key.json | base64)
# GOOGLE_APPLICATION_CREDENTIALS=  # 1순위 대안: 표준 GCP 변수

# ── 관리자 ──
ADMIN_EMAIL=admin@diah7m.com
ADMIN_PASSWORD=                # 관리자 비밀번호 (직접 설정)

# ── 프론트엔드 연결 (Vercel에 설정) ──
# VITE_API_URL=https://diah7m-server.onrender.com
