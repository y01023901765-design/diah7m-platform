# ═══════════════════════════════════════════════════════
# DIAH-7M Environment Configuration
# ═══════════════════════════════════════════════════════
# 복사: cp .env.example .env
# Render 배포 시: Dashboard → Environment → Bulk Add
# ═══════════════════════════════════════════════════════

# ── 프론트엔드 (Vite → Vercel) ──
# Render 서버 URL (배포 후 설정)
VITE_API_URL=https://diah7m-api.onrender.com

# ── 데이터 수집 API 키 (무료) ──
# ECOS: https://ecos.bok.or.kr → 인증키 발급
ECOS_API_KEY=
# KOSIS: https://kosis.kr/openapi → 인증키 발급
KOSIS_API_KEY=
# FRED: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
# AirKorea: https://data.go.kr → 대기오염정보 조회 서비스
AIRKOREA_API_KEY=
# WAQI: https://aqicn.org/data-platform/token/
WAQI_TOKEN=

# ── 인증 & 보안 ──
# JWT 시크릿 (프로덕션: openssl rand -hex 32)
JWT_SECRET=diah7m-dev-secret
# 관리자 비밀번호 (첫 실행 시 admin@diah7m.com 생성)
ADMIN_PASSWORD=
# 세션 만료
JWT_EXPIRY=24h

# ── 데이터베이스 ──
# SQLite (기본): 설정 불필요
# PostgreSQL: DATABASE_URL=postgresql://user:pass@host:5432/diah7m
DATABASE_URL=

# ── 서버 설정 ──
PORT=3700
NODE_ENV=development

# ── Render 배포 전용 ──
# 자동 설정됨: RENDER_GIT_COMMIT, RENDER_EXTERNAL_URL

# ── 위성 데이터 (Phase 2) ──
# Google Earth Engine: https://earthengine.google.com/
# GEE_SERVICE_ACCOUNT=
# GEE_PRIVATE_KEY=

# ── 결제 (Phase 3) ──
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
